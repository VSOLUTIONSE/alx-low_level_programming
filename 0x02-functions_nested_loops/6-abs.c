#include "main.h"

/**
 * _abs - Computes the absolute value of an intege.r
 * @n: The interger to be computed.
 *
 * Return: The absolute value of an interger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
